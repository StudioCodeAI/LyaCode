# LyaCloud Tracker

## 2026-06-18

- Status: in_progress
- Task: Prepare `E:\GitHub\lyacloud-main` as the clean Git workspace for Lya Cloud.
- Notes:
  - User defined `E:\GitHub\lyacloud-main` as the folder that will be sent to Git.
  - Target already exists and contains generated/local folders that should not be committed.
  - Prepare a clean Git-ready copy without `node_modules`, `dist`, `.agents`, `.codex`, or local environment artifacts.

- Status: in_progress
- Task: Scan identity variants across casing, separators, and code identifiers.
- Notes:
  - User requested a broader scan beyond exact matches: joined, separated, lowercase, uppercase, variables, and code identifiers.
  - Apply the requested pattern: product text `Lya Cloud`, CLI/package `lyacloud`, env vars `LYACLOUD_*`, and code identifiers `lyaCloud` / `LyaCloud`.
- Status: completed
- Result:
  - Scanned joined, separated, hyphenated, underscored, lowercase, uppercase, camelCase, PascalCase, env-var, CSS-var, file-name, and split-HTML wordmark variants.
  - Confirmed no physical file or folder still uses the old product identity.
  - Fixed split HTML wordmarks in the VS Code Control Center and Chat UI.
  - Normalized VS Code extension identifiers to `LyaCloud*` and `launchLyaCloud`.
  - Normalized CSS variables from `--oc-*` to `--lc-*`.
  - Normalized web theme storage key from `oc-theme` to `lyacloud-theme`.
  - Replaced stale Windows alias docs from `oc-*` to the actual Lya Cloud aliases: `lc`, `lya`, `lyacloud-local`, `lyacloud-provider`, and `lyacloud-help`.
  - Cleaned duplicate env fallbacks such as `LYACLOUD_* ?? LYACLOUD_*`.
  - Updated temporary test prefixes from `oc-test-*` to `lc-test-*`.
  - Remaining broad-search hit is technical text for opening `claude.ai`, not inherited product identity.
  - Validation passed: `bun run typecheck`, `bun run smoke`, `bun test src` inside `vscode-extension\lyacloud-vscode`, and `node bin\lyacloud --version`.

- Status: in_progress
- Task: Remove remaining OpenClaude product identity references and dangling command/config links.
- Notes:
  - User requested a full verification for renamed entrypoints and scripts so no changed name leaves a broken call path.
  - Replace OpenClaude identity with Lya Cloud across the project, while preserving legacy fallbacks only where required for migration or compatibility.
- Status: completed
- Result:
  - Performed a full source scan for `OpenClaude`, `openclaude`, `OPENCLAUDE`, `.openclaude`, and `Open Claude`.
  - Replaced remaining product identity references with Lya Cloud across source, scripts, docs, tests, web, Docker, and VS Code extension files.
  - Renamed physical files and folders that still carried the old product name, including tests, proto, web assets, and VS Code extension paths.
  - Fixed dangling references caused by renames, including Docker entrypoint, knip entry, VS Code theme path, gRPC proto path, launcher references, and config/profile paths.
  - Corrected invalid identifiers introduced by text replacement, such as commit message permission helpers.
  - Cleaned config migration logic so Lya Cloud uses `.lyacloud` and `LYACLOUD_CONFIG_DIR`, with only `.claude` retained as technical compatibility fallback.
  - Added `docs/LYA_IDENTITY.md` for the product identity and created internal memory file `C:\Gemini\NEURO_CORE\LYA_SYSTEM\memory\identity\LYA_CLOUD_PRODUCT_IDENTITY.md`.
  - Final scan result: zero matches for `OpenClaude`, `openclaude`, `OPENCLAUDE`, `.openclaude`, and `Open Claude` outside ignored generated/dependency folders.
  - Validation passed: `bun run typecheck`, `bun run smoke`, and `node bin\lyacloud --version`.

- Status: in_progress
- Task: Build the first functional Lya Cloud foundation from the current CLI base.
- Notes:
  - User assigned Studio CodeAI/Lya Cloud product direction and approved developing from the current base.
  - Legacy `LyaCode/` folder has been removed by the user.
  - Scope for this pass: product identity, launcher, config naming, minimal docs, build validation.
- Status: completed
- Result:
  - Created the first functional Lya Cloud foundation on top of the current CLI base.
  - Renamed the runtime package identity to `@studiocodeai/lyacloud` and the executable to `lyacloud`.
  - Added the `bin/lyacloud` launcher and Windows alias script.
  - Updated primary terminal branding, startup screen, help title, product constants, README, roadmap, build scripts, config directory, provider profile naming, command copy, and core URLs.
  - Preserved provider/compatibility references where `Claude Code` is still technical context.
  - Added migration fallback from `.openclaude` and `.claude` into `.lyacloud`.
  - Validation passed: `bun install`, `bun run build`, `bun run smoke`, `bun run typecheck`, and `node bin\lyacloud --version`.
  - Remaining phase: deep cleanup of inherited OpenClaude traces in secondary docs, tests, web, VS Code extension, bridge/proto, and lower-traffic command text.

- Status: in_progress
- Task: Record product identity boundary for Lya Cloud migration.
- Notes:
  - User decision: `Claude Code` remains when it means provider or technical compatibility.
  - User decision: `OpenClaude` must leave as product identity and become `Lya Cloud`, a Studio CodeAI product.
- Status: completed
- Result:
  - Updated `docs/LYA_CLOUD_ROADMAP.md` with a dedicated identity rule.
  - Rebrand boundary is now explicit: remove `OpenClaude` product identity, preserve `Claude Code` technical/provider references.

- Status: in_progress
- Task: Create the Lya Cloud project foundation roadmap and identity migration analysis.
- Notes:
  - Target identity from user: `Lya Cloud`, a Studio CodeAI product.
  - Visual target is the current OpenClaude terminal startup screen changed to Lya Cloud identity.
  - Analyze source identity references before planning implementation phases.
- Status: completed
- Result:
  - Created `docs/LYA_CLOUD_ROADMAP.md`.
  - Identified first identity targets: `package.json`, `bin/openclaude`, `src/constants/brand.ts`, `src/components/StartupScreen.ts`, `src/constants/product.ts`, `scripts/build.ts`, docs, web, and VS Code extension.
  - Recommended phased migration to avoid breaking provider compatibility, config paths, and inherited `CLAUDE_CODE_*` behavior.

- Status: in_progress
- Task: Analyze the imported legacy `LyaCode` folder as reference material for the new LyaCode CLI.
- Notes:
  - User clarified the old project should not become the direction of the new project.
  - Treat legacy content as reference and possible source of reusable identity or isolated components only.
- Status: completed
- Result:
  - Legacy `LyaCode` is a Tauri 2 + React/Vite desktop app, not a terminal-first CLI.
  - Useful references: product identity, slogans, theme ideas, provider UX, command palette, AI overlay, secure key storage direction, skill and agent concepts.
  - Do not use as the main architecture for the current project.
  - Current project direction remains: transform the OpenClaude-based CLI into `LyaCode`, an agentic terminal by Studio CodeAI.
  - Before initializing Git for the new project, decide whether to keep `LyaCode/` in-repo as reference or move/archive it because it contains its own `.git`, `node_modules`, and generated/build artifacts.

- Status: in_progress
- Task: Remove the inherited invalid `.git` directory after user approval.
- Notes:
  - User confirmed this Git metadata is not ours and can be deleted.
  - Target path verified as `C:\MASTER\lyacloud-main\.git`.
- Status: completed
- Result:
  - Removed `C:\MASTER\lyacloud-main\.git`.
  - Verified that `.git` is no longer present.

- Status: in_progress
- Task: Analyze the current repository as the starting point for LyaCloud.
- Notes:
  - Created tracker because `.lya_tracker.md` was not present at task start.
  - Initial scan shows an OpenClaude-based TypeScript/Bun CLI repository.

- Status: completed
- Result:
  - Repository is an OpenClaude fork/base with a mature TypeScript/Bun CLI.
  - Runtime is available locally: Node 22.22.2, Bun 1.3.14, npm 10.9.7.
  - Dependencies and build output are not installed yet: `node_modules` and `dist` are missing.
  - `.git` exists as an empty directory without `HEAD`, so Git is not initialized correctly.
  - Major reusable areas: React/Ink terminal UI, provider profiles, file/shell tools, MCP, agents/tasks, memory, docs, VS Code extension, gRPC server.
  - Major rebrand areas: package/bin names, OpenClaude constants, config paths, docs, web site, launcher scripts, extension identity, issue URLs, telemetry/product labels.
